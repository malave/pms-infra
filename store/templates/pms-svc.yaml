---
apiVersion: v1
kind: Service
metadata:
  labels:
    app: {{ .Values.store.name }}
  name: {{ .Values.store.name }}
  namespace: {{ .Values.ns }}
spec:
  ports:
  - port: 8080
    protocol: TCP
    targetPort: 8080
  selector:
    app: {{ .Values.store.name }}
---
